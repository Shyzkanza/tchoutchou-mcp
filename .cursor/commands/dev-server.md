---
description: 🌟 Lance le serveur MCP dev avec hot-reload
---

Lance le serveur de développement MCP avec hot-reload. Kill automatiquement le process existant sur le port.

**Workflow recommandé** : Lancer d'abord `tunnel-only` dans un terminal séparé (et le laisser tourner), puis cette commande.

```bash
cd $WORKSPACE && npm run dev
```

