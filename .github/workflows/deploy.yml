name: Deploy TchouTchou MCP to VPS

on:
  push:
    branches:
      - main
  workflow_dispatch:

# Annule les déploiements précédents
concurrency:
  group: deploy-tchoutchou-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Job 1: Tests et vérifications
  test:
    name: Run Tests & Type Checks
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
    
    - name: Install dependencies (main)
      run: npm ci --ignore-scripts
    
    - name: Install dependencies (UI)
      run: |
        cd web
        npm ci --ignore-scripts
        cd ..
    
    - name: TypeScript Type Check (main)
      run: npx tsc --noEmit
    
    - name: TypeScript Type Check (UI)
      run: |
        cd web
        npx tsc --noEmit
        cd ..
    
    - name: Build Test
      run: |
        npm run build
        echo "✅ Build successful"
  
  # Job 2: Déploiement via Portainer
  deploy:
    name: Deploy via Portainer
    runs-on: ubuntu-latest
    needs: test  # Attend que les tests passent
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    # Redéploiement via Portainer API
    - name: Redeploy via Portainer Git
      run: |
        echo "🔐 Authentification à Portainer..."
        
        # Authentification Portainer
        AUTH_TOKEN=$(curl -s -X POST \
          "${{ secrets.PORTAINER_URL }}/api/auth" \
          -H "Content-Type: application/json" \
          -d '{"Username":"${{ secrets.PORTAINER_USERNAME }}","Password":"${{ secrets.PORTAINER_PASSWORD }}"}' \
          | jq -r '.jwt')
        
        if [ -z "$AUTH_TOKEN" ] || [ "$AUTH_TOKEN" = "null" ]; then
          echo "❌ Erreur d'authentification Portainer"
          exit 1
        fi
        
        echo "✅ Authentification Portainer réussie"
        
        # Git redeploy
        echo "🔄 Redéploiement de la stack depuis Git..."
        
        RESPONSE=$(curl -s -w "\n%{http_code}" -X PUT \
          "${{ secrets.PORTAINER_URL }}/api/stacks/${{ secrets.PORTAINER_STACK_ID }}/git/redeploy?endpointId=${{ secrets.PORTAINER_ENDPOINT_ID }}" \
          -H "Authorization: Bearer $AUTH_TOKEN" \
          -H "Content-Type: application/json" \
          -d '{
            "prune": false,
            "pullImage": true,
            "repositoryAuthentication": true,
            "repositoryUsername": "${{ github.actor }}",
            "repositoryPassword": "${{ secrets.GITHUB_TOKEN }}",
            "repositoryReferenceName": "refs/heads/main"
          }')
        
        HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
        BODY=$(echo "$RESPONSE" | sed '$d')
        
        if [ "$HTTP_CODE" -ge 200 ] && [ "$HTTP_CODE" -lt 300 ]; then
          echo "✅ Stack redéployée depuis Git (HTTP $HTTP_CODE)"
        else
          echo "❌ Erreur lors du redéploiement (HTTP $HTTP_CODE)"
          echo "$BODY"
          exit 1
        fi
        
        # Attendre le redémarrage complet
        echo "⏳ Attente de 30 secondes pour le démarrage du conteneur..."
        sleep 30
    
  
  # Job 3: Publish to npm
  publish-npm:
    name: Publish to npm
    runs-on: ubuntu-latest
    needs: test  # Attend les tests
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        registry-url: 'https://registry.npmjs.org'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci --ignore-scripts
    
    - name: Install UI dependencies
      run: |
        cd web
        npm ci --ignore-scripts
        cd ..
    
    - name: Build package
      run: npm run build
    
    - name: Publish to npm
      run: npm publish --access public
      env:
        NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
  
  # Job 4: Health Check
  health-check:
    name: Health Check
    runs-on: ubuntu-latest
    needs: deploy  # Attend le déploiement
    
    steps:
    - name: Test API Health
      run: |
        echo "🔍 Test de l'API via Traefik..."
        
        # Test avec retry en cas d'échec temporaire
        for i in {1..5}; do
          echo "🔄 Tentative $i/5..."
          
          if curl -f -k https://tchoutchou-mcp.rankorr.red/health; then
            echo ""
            echo "✅ API accessible via Traefik !"
            
            # Test endpoint MCP
            echo "🔍 Test de l'endpoint MCP..."
            curl -f -k https://tchoutchou-mcp.rankorr.red/mcp || true
            
            exit 0
          else
            echo "⚠️ Échec tentative $i, attente 10s..."
            sleep 10
          fi
        done
        
        echo "❌ L'API n'est pas accessible après 5 tentatives"
        exit 1
    
    - name: Deployment Summary
      if: always()
      run: |
        if [ ${{ job.status }} == 'success' ]; then
          echo "🎉 Déploiement réussi: https://tchoutchou-mcp.rankorr.red"
          echo "📍 Endpoint MCP: https://tchoutchou-mcp.rankorr.red/mcp"
          echo "💚 Health: https://tchoutchou-mcp.rankorr.red/health"
        else
          echo "❌ Health check échoué"
        fi
